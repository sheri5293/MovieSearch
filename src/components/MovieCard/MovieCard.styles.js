import styled from "styled-components";

export const CardContainer = styled.div`
  img {
    width: 100%;
    height: auto;
    transition: transform 0.2s;
  }
  img:hover {
    transform: scale(1.1);
  }
`;
